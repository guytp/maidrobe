/**
 * Shared components barrel export.
 *
 * This directory is reserved for reusable UI components that are shared
 * across multiple features. Components specific to a single feature should
 * live within that feature's directory.
 *
 * Examples of shared components:
 * - Button, Input, Card (design system components)
 * - Loading spinners, error boundaries
 * - Custom headers, footers
 *
 * Currently empty - components will be added as needed.
 */

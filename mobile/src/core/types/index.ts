/**
 * Core shared types used across multiple features.
 *
 * This module re-exports types that are shared between features to avoid
 * circular dependencies and provide a single import point.
 *
 * @module core/types
 */

export * from './capture';
